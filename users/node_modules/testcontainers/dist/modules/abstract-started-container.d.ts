/// <reference types="node" />
import { StartedTestContainer, StopOptions, StoppedTestContainer } from "testcontainers/dist/test-container";
import { Host } from "testcontainers/dist/docker-client-instance";
import { Port } from "testcontainers/dist/port";
import { Command, ContainerName, ExecResult } from "testcontainers/dist/docker-client";
import { Id as ContainerId } from "testcontainers/dist/container";
import { Readable } from "stream";
export declare class AbstractStartedContainer {
    protected readonly startedTestContainer: StartedTestContainer;
    constructor(startedTestContainer: StartedTestContainer);
    stop(options?: Partial<StopOptions>): Promise<StoppedTestContainer>;
    getHost(): Host;
    getMappedPort(port: Port): Port;
    getName(): ContainerName;
    getId(): ContainerId;
    getNetworkNames(): string[];
    getNetworkId(networkName: string): string;
    getIpAddress(networkName: string): string;
    exec(command: Command[]): Promise<ExecResult>;
    logs(): Promise<Readable>;
}
