import { Log, WaitStrategy } from "testcontainers/dist/wait-strategy";
export declare class Wait {
    static forLogMessage(message: Log | RegExp): WaitStrategy;
    static forHealthCheck(): WaitStrategy;
}
