"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FixedPortClient = exports.RandomPortClient = void 0;
const get_port_1 = __importDefault(require("get-port"));
class RandomPortClient {
    preferredRandomPort(min, max) {
        // min and max included
        return Math.floor(Math.random() * (max - min + 1) + min);
    }
    getPort() {
        return get_port_1.default({ port: this.preferredRandomPort(10000, 65535) });
    }
}
exports.RandomPortClient = RandomPortClient;
class FixedPortClient {
    constructor(ports) {
        this.ports = ports;
        this.portIndex = 0;
    }
    getPort() {
        return Promise.resolve(this.ports[this.portIndex++]);
    }
}
exports.FixedPortClient = FixedPortClient;
